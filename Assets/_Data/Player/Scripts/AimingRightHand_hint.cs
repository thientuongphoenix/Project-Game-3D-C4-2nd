using UnityEngine;

public class AimingRightHand_hint : SaiMonoBehaviour
{
    protected override void ResetValue()
    {
        base.ResetValue();
        transform.localPosition = new Vector3(-20.84226f, 1.384274f, -21.81131f);
        transform.localRotation = Quaternion.Euler(0, 0, 0);
    }
}
